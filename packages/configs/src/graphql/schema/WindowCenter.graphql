#import Site, PosInt from "lucuma-schemas/odb"

type WindowCenter {
  site: Site!
  x: Float!
  y: Float!
}

type Query {
  windowCenter(site: Site!): WindowCenter! @deprecated(reason: "Use `calParams` query instead")
}

type Mutation {
  setWindowCenter(site: Site!, x: Float, y: Float): WindowCenter!
    @deprecated(reason: "Use `createCalParams` mutation instead")
}
